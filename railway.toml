[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn wsgi:app --bind 0.0.0.0:${PORT:-8000} --workers 2"
healthcheckPath = "/.well-known/wikihub.json"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[mounts]]
mountPath = "/data/repos"
